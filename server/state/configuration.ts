import * as _configImport from '../config/config.json'
import Configuration from '../types/Configuration'

const config: Configuration = _configImport

// Would not be useful
// export const IDType = config.types.product.idType

export default config
